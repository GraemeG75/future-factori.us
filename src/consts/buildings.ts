/** Base harvest rate in units per tick at building level 1. */
export const BASE_HARVEST_RATE = 0.1;
/** Max distance (world units) from a spot to snap a harvester placement. */
export const HARVESTER_SNAP_RADIUS = 8;
/** Minimum distance (world units) between any two non-harvester buildings. */
export const MIN_BUILDING_SEPARATION = 4;
export const TERRAIN_HEIGHT_PRECISION = 100;
/** Number of cargo capsules spread across each route. */
export const CARGO_COUNT = 3;
/** Base global inventory capacity multiplier for storage depots (per level). */
export const STORAGE_DEPOT_CAPACITY_PER_LEVEL = 100;
/** Smoke particle colour for building animations. */
export const SMOKE_COLOR = 0x999999;

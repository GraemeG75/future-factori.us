/** Maximum random demand shift per update call. */
export const DEMAND_DRIFT = 0.05;
/** Minimum demand floor. */
export const DEMAND_MIN = 0.1;
/** Maximum demand ceiling. */
export const DEMAND_MAX = 1.0;
